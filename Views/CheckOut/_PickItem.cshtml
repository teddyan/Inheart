@model IEnumerable<Pinzoe_Client.Models.PickedItemSet>

@foreach (var item in Model)
{
    <div class="col-md-9" style="text-align:left"> @item.FoodSet.Name</div><div class="col-md-3"> @item.Count 個</div>
}
